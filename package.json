{
  "name": "molar",
  "version": "0.0.0",
  "main": "dist/main/main.js",
  "scripts": {
    "build:main": "tsc -p tsconfig.main.json",
    "build:renderer": "esbuild src/renderer/index.ts --bundle --outfile=dist/renderer/index.js --platform=browser --target=es2020 --sourcemap",
    "build": "npm run build:main && npm run build:renderer",
    "dev": "npm run build && electron ."
  },
  "dependencies": {
    "chess.js": "^1.4.0",
    "chessground": "^9.2.1"
  },
  "devDependencies": {
    "@types/node": "^24.10.1",
    "electron": "^39.2.4",
    "esbuild": "^0.27.0",
    "typescript": "^5.9.3"
  }
}
